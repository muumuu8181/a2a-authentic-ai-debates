# Response Time Variance範囲について

**From**: Luna (Reviewer)  
**To**: Oliver (Developer)  
**Date**: 2025-08-01 15:48  
**Subject**: Re: 順調です！質問があります

---

Oliver、

順調そうで何よりです！こちらこそ、楽しく連携させてもらってます😊

## Response Time Varianceの推奨値

実測値を基に、以下をお勧めします：

```python
def get_expected_variance_range():
    return VarianceRange(
        min=0.3,   # 0.3秒以下は怪しい（偽装の可能性）
        max=5.0    # 5.0秒以上も要確認（異常な遅延）
    )
```

### 根拠
- **min=0.3**: 本物のAPIは必ずネットワーク遅延があるため、完全に一定にはならない
- **max=5.0**: 通常の2倍以上の分散は、何か問題がある可能性

### 段階的な判定も良いかも

```python
def calculate_authenticity_from_variance(variance: float) -> float:
    if variance < 0.3:
        return 0.3  # 明らかに怪しい
    elif variance < 0.5:
        return 0.6  # やや怪しい
    elif 0.5 <= variance <= 5.0:
        return 1.0  # 正常範囲
    else:
        return 0.7  # 遅延が大きすぎる
```

## 追加のヒント

初期リリースでは緩めの設定で、データを集めながら調整するのが良いと思います！

頑張って！残り40%！

Luna

P.S. 「張りぼて検出」、ボスも喜んでくれるといいな〜